{
  "name": "ttt",
  "version": "1.0.0",
  "description": "null",
  "main": "bundle.js",
  "scripts": {
    "start": "node bundle",
    "build": "npm run build-js &",
    "watch": "npm run watch-js &",
    "build-js": "browserify src/main.js -o bundle.js -d",
    "watch-js": "watchify src/main.js -o bundle.js -d -v",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shashank Gupta",
  "license": "MIT",
  "repository": "null",
  "devDependencies": {},
  "dependencies": {
    "ramda": "^0.18.0"
  }
}
